extends ../layout

block content
    include ../mixins/cardCard
    div.container-card
        div.content
            div.deck-name=deck.deck_name
            if user && user.is('Deck Manager')
                a.button(href=`/deck/${deck.id}/edit`) Edit

            ul.card-list
                each card in deck.cards
                    li
                        +cardCard(card)