extends ../layout

block content
    include ../mixins/deckDeck
    div.decks-header-title Here's all of the premade decks
    div
        a.button.primary(href='/deck/add') Create a Deck

    ul.deck-list
        each deck in decks
            li
                +deckDeck(deck)